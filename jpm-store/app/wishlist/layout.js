/* Wishlist page layout — adds metadata (server-side) while page.js remains client */
export const metadata = {
    title: 'Wishlist — JPM Store',
    description: 'Your saved products on JPM Store. Review and add to cart.',
};

export default function WishlistLayout({ children }) {
    return children;
}
